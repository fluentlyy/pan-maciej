/* const video = document.querySelector(".porsche__video");

window.addEventListener("mousemove", (e) => {
  const { innerWidth, innerHeight } = window;
  const x = (e.clientX - innerWidth / 2) / 150;
  const y = (e.clientY - innerHeight / 2) / 150;

  video.style.transform = `translate(-50%, -50%) rotateX(${y}deg) rotateY(${x}deg)`;
});

// Плавне повернення до стандартного стану при виході миші за межі екрану
window.addEventListener("mouseleave", () => {
  video.style.transform = "translate(-50%, -50%) rotateX(0deg) rotateY(0deg)";
});
 */
